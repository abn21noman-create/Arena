#!/bin/bash
# HSC Ultimate — Icon generator (PRO version with book + bookmark)
# Run: bash scripts/generate-icons.sh
# This regenerates all icons from a fresh composition.
exec "$(dirname "$0")/generate-icons-pro.sh" "$@"
